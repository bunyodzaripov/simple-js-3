let a = prompt("Istalgan a-son kiriting: ");
let b = prompt("Istalgan b-son kiriting: ");
let c = prompt("Istalgan c-son kiriting: ");
let d = prompt("Istalgan d-son kiriting: ");

if (a >= 0 && b >= 0 && c >= 0 && d >= 0) {
   document.write("To'gri");
} else {
   document.write("Noto'gri");
}
